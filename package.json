{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "bump": "lerna version --no-push"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "version": "1.0.0",
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
