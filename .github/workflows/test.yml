name: Test

on:
  push:
    paths:
      - packages/**

jobs:
  test:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        derp: [a, b]
    name: "Test the thing ${{derp}}"
    steps:
      - run: echo "Tested!"
